/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  trailingSlash: true,
  basePath: '',
  // Assurez-vous que ces options sont compatibles avec votre configuration React
}

module.exports = nextConfig